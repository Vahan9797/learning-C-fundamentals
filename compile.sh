#!/usr/bin/env bash

# destination should be source-files folder
if [ $# -eq 0 ]; then
	#TODO: if there is no argument passed, try and compile all files
else
    #TODO: if arguments are passed, compile only them
fi